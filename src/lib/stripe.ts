// src/lib/stripe.ts
// Platzhalter, bis Stripe wirklich eingebaut wird

export const stripe = null;

// Beispiel-Funktion, die später durch echte Stripe-Calls ersetzt wird
export async function createCheckoutSession() {
  throw new Error("Stripe ist noch nicht implementiert.");
}
